{
  "name": "xarajatlar-ipe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npx prisma db push --schema=./server/prisma/schema.prisma --accept-data-loss && npx prisma generate --schema=./server/prisma/schema.prisma",
    "vercel-build": "npx prisma db push --schema=./server/prisma/schema.prisma --accept-data-loss && npx prisma generate --schema=./server/prisma/schema.prisma && cd client && npm install && npm run build"
  },
  "dependencies": {
    "@google/genai": "^2.21.0",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "grammy": "^1.35.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.1",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^22.13.10",
    "prisma": "^5.22.0",
    "typescript": "^5.8.2"
  }
}
